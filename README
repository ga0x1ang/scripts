some useless scripts ...

**linkremover** - parse an csv file and get group the urls by ips
**link-removal** - walk through all files in a directory, and replace a specfic string
**sys-size-viewer** - I'm always forgetting the size of different data types ...
